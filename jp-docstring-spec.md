# 日本語 Docstring 外部仕様書

## 1. 目的

`src/yklibpy` 配下の Python ソースに対して、クラス定義、メソッド定義、関数定義の先頭へ配置する docstring の記述方針を統一する。

既存 docstring は追記ではなく置換を前提とし、定義ごとに役割、前提、戻り値、失敗条件が読み取れる状態を完成条件とする。

## 2. 対象

- `src/yklibpy/common/*.py`
- `src/yklibpy/db/*.py`
- `src/yklibpy/tomlop/*.py`
- `src/yklibpy/htmlparser/*.py`
- `src/yklibpy/htmlparser/misc/*.py`
- `src/yklibpy/command/*.py`
- `src/yklibpy/cli/*.py`
- `src/yklibpy/config/*.py`

対象定義の種類は次のとおり。

- すべてのトップレベル `class`
- すべてのクラスメソッド、インスタンスメソッド、`__init__`
- すべてのトップレベル `def`
- ネストした補助クラスと補助関数

## 3. 記述ルール

- 日本語で記述する。ただしクラス名、メソッド名、関数名、引数名は英語識別子をそのまま用いる。
- PEP 257 ベースの簡潔な docstring とする。
- 1 行で十分な定義は 1 文または 1 段落で記述する。
- 引数、戻り値、失敗条件の説明が複数必要な定義だけ Google style の `Args`、`Returns`、`Raises` を使う。
- 型の説明は型ヒントに任せ、docstring では意味、制約、前提、利用意図を書く。
- 実装詳細の逐語説明は避け、呼び出し側が知るべき振る舞いを優先する。

## 4. モジュール別の書き分け

### `common`

- ユーティリティ系は「何を正規化するか」「どの条件で空値や失敗を返すか」を明記する。
- `Env` は設定の読み方と探索対象の決め方を中心に書く。
- `Util` は文字列処理、パス操作、TSV/YAML 補助の責務が分かる表現にする。

### `db`

- 永続化先、内部保持データ、読み書きタイミングを明記する。
- `AppStore` と `Storex` は OS ごとの保存先解決やファイル種別の違いが分かるように書く。
- `DbYaml` は読み込み時のエンコーディング推定や失敗時の扱いを必要に応じて補足する。

### `tomlop`

- 変換元と変換先、差分比較の観点、出力される成果物を明記する。
- `Tomlop` は CLI 実行、比較、変換のどの処理を担当するかが分かる表現にする。
- `FileItem` はパス解決と `Storex` 連携の薄いラッパーであることを簡潔に示す。

### `htmlparser`

- HTML 解析、リンク抽出、補助オブジェクト生成の役割が分かるように書く。
- `Scraper` 系は基底クラスとしての責務と、サブクラス実装前提のメソッドを区別して書く。
- `misc` 配下は保持するタグ情報や表示用整形内容を中心に書く。

### `command` / `cli` / `config`

- `Command` 系はコマンド実行結果、終了条件、回数管理の責務を明記する。
- `Cli` は `argparse` ラッパーとしての役割を簡潔に書く。
- `AppConfig` は共有定数とファイル種別判定の提供者であることを示す。

## 5. 完了条件

- 対象定義すべてに日本語 docstring がある。
- 既存 docstring は仕様に合わせた表現へ置換されている。
- 複雑な定義だけが Google style を使い、単純な定義では冗長な節を増やしていない。
- docstring が型ヒントの焼き直しになっていない。
- 編集したファイルで lint エラーを新規発生させていない。
